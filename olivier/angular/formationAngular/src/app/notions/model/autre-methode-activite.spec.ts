import { AutreMethodeActivite } from './autre-methode-activite';

describe('AutreMethodeActivite', () => {
  it('should create an instance', () => {
    expect(new AutreMethodeActivite()).toBeTruthy();
  });
});
